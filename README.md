

```bash
> vcvars64
> meson setup builddir --prefix D:\devtools\openh264.2.4.0 --buildtype release
> meson setup builddir --prefix D:\devtools\openh264.2.4.0 --buildtype debug
> ninja -C builddir
> ninja install -C builddir
```